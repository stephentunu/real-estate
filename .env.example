# =============================================================================
# JASTON REAL ESTATE - UNIFIED ENVIRONMENT CONFIGURATION EXAMPLE
# =============================================================================
# This file contains all environment variables for both backend and frontend.
# Variables prefixed with VITE_ are automatically exposed to the frontend.
# 
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.example .env
# 2. Update the values below with your actual configuration
# 3. Never commit .env files to version control
# =============================================================================

# -----------------------------------------------------------------------------
# APPLICATION ENVIRONMENT
# -----------------------------------------------------------------------------
# Environment type: development, staging, production
APP_ENV=development

# Application metadata
APP_NAME="Jaston Real Estate"
APP_VERSION=1.0.0

# -----------------------------------------------------------------------------
# DJANGO BACKEND CONFIGURATION
# -----------------------------------------------------------------------------
# Security - IMPORTANT: Generate a new secret key for production
# Use: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=your-secret-key-here

# Debug mode - MUST be False in production
DEBUG=True

# Allowed hosts - Update for production domains
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# Server Configuration
BACKEND_PORT=8000
BACKEND_HOST=0.0.0.0

# Database Configuration (Development - SQLite)
DB_ENGINE=django.db.backends.sqlite3
DB_NAME=db.sqlite3

# Database Configuration (Production - PostgreSQL)
# Uncomment and configure for production
# DB_ENGINE=django.db.backends.postgresql
# DB_NAME=jaston_db
# DB_USER=postgres
# DB_PASSWORD=your_secure_password
# DB_HOST=localhost
# DB_PORT=5432

# CORS Configuration - Update for production domains
CORS_ALLOWED_ORIGINS=http://localhost:8080,http://localhost:8081,http://127.0.0.1:8080,http://127.0.0.1:8081

# Email Configuration
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
DEFAULT_FROM_EMAIL=noreply@jastonrealestate.com
SERVER_EMAIL=server@jastonrealestate.com

# Static and Media Files
STATIC_ROOT=staticfiles
MEDIA_ROOT=media

# -----------------------------------------------------------------------------
# FRONTEND CONFIGURATION
# -----------------------------------------------------------------------------
# Vite Development Server
VITE_PORT=8080
VITE_HOST=localhost

# API Configuration - Update for production
VITE_API_URL=http://localhost:8000/api/v1
VITE_WS_URL=ws://localhost:8000/ws

# Supabase Configuration (Optional - if using additional services)
# Get these values from your Supabase project dashboard
VITE_SUPABASE_PROJECT_ID=your_project_id
VITE_SUPABASE_PUBLISHABLE_KEY=your_publishable_key
VITE_SUPABASE_URL=https://your_project_id.supabase.co

# Application Features
VITE_ENABLE_ANALYTICS=false
VITE_ENABLE_DEBUG_MODE=true

# -----------------------------------------------------------------------------
# THIRD-PARTY SERVICES
# -----------------------------------------------------------------------------
# Redis Configuration (for caching and sessions)
# Install Redis: sudo apt install redis-server
REDIS_URL=redis://localhost:6379/0

# Celery Configuration (for background tasks)
CELERY_BROKER_URL=redis://localhost:6379/1
CELERY_RESULT_BACKEND=redis://localhost:6379/2

# Logging Configuration
LOG_LEVEL=INFO
LOG_FILE=logs/jaston.log

# -----------------------------------------------------------------------------
# PRODUCTION ENVIRONMENT VARIABLES
# -----------------------------------------------------------------------------
# Uncomment and configure these for production deployment

# SSL/HTTPS Configuration
# SECURE_SSL_REDIRECT=True
# SECURE_HSTS_SECONDS=31536000
# SECURE_HSTS_INCLUDE_SUBDOMAINS=True
# SECURE_HSTS_PRELOAD=True
# SECURE_CONTENT_TYPE_NOSNIFF=True
# SECURE_BROWSER_XSS_FILTER=True
# SESSION_COOKIE_SECURE=True
# CSRF_COOKIE_SECURE=True

# Production Email Configuration (Example with SMTP)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your_email@gmail.com
# EMAIL_HOST_PASSWORD=your_app_password

# Cloud Storage (Example with AWS S3)
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# AWS_STORAGE_BUCKET_NAME=your_bucket_name
# AWS_S3_REGION_NAME=us-east-1

# Monitoring and Analytics
# SENTRY_DSN=your_sentry_dsn
# GOOGLE_ANALYTICS_ID=your_ga_id